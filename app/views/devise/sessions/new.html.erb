<main class='mx-auto p-4 sm:max-w-sm'>
  <h1 class='text-center text-xl font-bold leading-tight tracking-tight my-4'>
    Log in to <%= app_name %>
  </h1>

  <%= form_for(resource, as: resource_name, url: session_path(resource_name), html: { class: 'space-y-4' }) do |f| %>
    <div>
      <%= f.label :email, class: 'block text-sm font-medium mb-2' %>
      <%= f.email_field :email, autofocus: true, autocomplete: "email", class: 'form-control' %>
    </div>

    <div>
      <%= f.label :password, class: 'block text-sm font-medium mb-2' %>
      <%= f.password_field :password, autocomplete: "current-password", class: 'form-control' %>
    </div>

    <% if devise_mapping.rememberable? %>
      <div class="flex items-center">
        <%= f.check_box :remember_me, class: 'form-checkbox' %>
        <%= f.label :remember_me, class: 'ml-3 text-gray-300 text-sm' %>
        <% if devise_mapping.recoverable? %>
          <%= link_to "Forgot password?", new_password_path(resource_name), class: 'link-primary block ml-auto text-sm' %>
        <% end %>
      </div>
    <% end %>

    <%= f.submit "Log in", class: 'btn-primary' %>

    <div class='text-sm'>
      <span class='text-secondary'>Don't have an account yet?</span>
      <%= link_to "Sign up", new_registration_path(resource_name), class: 'link-primary' %>
    </div>
  <% end %>
</main>
